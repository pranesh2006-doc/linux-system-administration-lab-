df -h       → Disk/filesystem space
du -sh      → Directory size
du -sh ~/*  → Find directory sizes
lsblk       → Disks and partitions
lsblk -f    → Filesystem details
mount       → Mounted filesystems
umount      → Unmount filesystem
